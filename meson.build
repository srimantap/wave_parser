project('wave parser', 'c',
    version : '1.0.0',
    meson_version : '>=0.54',
    default_option : [ 'warning_level=1',
                       'buildtype=debugoptimized' ])

cc = meson.get_compiler('c')

subdir('src')
subdir('tests')


